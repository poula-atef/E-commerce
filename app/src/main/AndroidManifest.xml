<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_commerce">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_groceries"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/ic_groceries"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecommerce">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


        <activity android:name=".UI.MapActivity"/>
        <activity android:name=".UI.CalenderActivity" />
        <activity android:name=".UI.CheckOutActivity" />
        <activity android:name=".UI.ItemQrCodeActivity" />
        <activity android:name=".UI.ScannerActivity" />
        <activity android:name=".UI.MenuActivity" />
        <activity android:name=".UI.CardActivity" />
        <activity android:name=".UI.ItemProfileActivity" />
        <activity android:name=".UI.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>